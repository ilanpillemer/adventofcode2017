import kotlin.math.max
var m = 0
var o : Int = 0
var wnh : Int = 0
var ugc : Int = 0
var baj : Int = 0
var xml : Int = 0
var wfu : Int = 0
var l : Int = 0
var sqf : Int = 0
var vke : Int = 0
var fy : Int = 0
var b : Int = 0
var rz : Int = 0
var jyi : Int = 0
var y : Int = 0
var xbp : Int = 0
var az : Int = 0
var pp : Int = 0
var k : Int = 0
var sb : Int = 0
var e : Int = 0
var u : Int = 0
var c : Int = 0
var sz : Int = 0
var tme : Int = 0
var eia : Int = 0
fun one() {
    if (wnh < -1) {
        o -= -427;m = max(m, o)
    }
    if (baj <= 3) {
        ugc -= -128;m = max(m, ugc)
    }
    if (xml <= -1) {
        ugc += 294;m = max(m, ugc)
    }
    if (wfu < -2) {
        ugc += -277;m = max(m, ugc)
    }
    if (sqf == 0) {
        l += 302;m = max(m, l)
    }
    if (wnh > -6) {
        l += -940;m = max(m, l)
    }
    if (fy >= -3) {
        vke += 687;m = max(m, vke)
    }
    if (ugc > 118) {
        b += 976;m = max(m, b)
    }
    if (jyi < 5) {
        rz -= 201;m = max(m, rz)
    }
    if (l >= -647) {
        y -= -960;m = max(m, y)
    }
    if (xbp <= 3) {
        baj -= 879;m = max(m, baj)
    }
    if (b <= 981) {
        vke -= 360;m = max(m, vke)
    }
    if (baj <= -875) {
        az -= 237;m = max(m, az)
    }
    if (pp > -7) {
        wnh -= 818;m = max(m, wnh)
    }
    if (ugc >= 121) {
        baj -= -434;m = max(m, baj)
    }
    if (k < 1) {
        az += 501;m = max(m, az)
    }
    if (sb < 4) {
        y += 43;m = max(m, y)
    }
    if (o < 3) {
        e -= -195;m = max(m, e)
    }
    if (u == 0) {
        y += 729;m = max(m, y)
    }
    if (xbp <= 0) {
        l += 993;m = max(m, l)
    }
    if (o < 5) {
        u += -252;m = max(m, u)
    }
    if (k == 0) {
        k += 547;m = max(m, k)
    }
    if (c != 4) {
        e += -781;m = max(m, e)
    }
    if (k >= 545) {
        y += -963;m = max(m, y)
    }
    if (xml != -9) {
        k += -906;m = max(m, k)
    }
    if (rz <= -210) {
        k -= 394;m = max(m, k)
    }
    if (baj < -442) {
        l -= 676;m = max(m, l)
    }
    if (sqf >= -7) {
        rz += -376;m = max(m, rz)
    }
    if (sz >= 6) {
        l += -388;m = max(m, l)
    }
    if (jyi < -1) {
        jyi -= 464;m = max(m, jyi)
    }
    if (rz == -577) {
        sb -= -397;m = max(m, sb)
    }
    if (b != 975) {
        jyi -= -391;m = max(m, jyi)
    }
    if (xbp != 2) {
        rz -= 85;m = max(m, rz)
    }
    if (az != 255) {
        rz -= -971;m = max(m, rz)
    }
    if (vke < 337) {
        e += 622;m = max(m, e)
    }
    if (l < -318) {
        pp -= 506;m = max(m, pp)
    }
    if (az < 262) {
        k += -471;m = max(m, k)
    }
    if (vke <= 331) {
        wnh -= -346;m = max(m, wnh)
    }
    if (o != 1) {
        sb -= 23;m = max(m, sb)
    }
    if (k != -349) {
        y -= -29;m = max(m, y)
    }
    if (sb < 377) {
        fy += 83;m = max(m, fy)
    }
    if (tme < 3) {
        c -= -283;m = max(m, c)
    }
    if (e == 34) {
        az -= 852;m = max(m, az)
    }
    if (b == 976) {
        fy += 935;m = max(m, fy)
    }
    if (e <= 44) {
        pp -= 553;m = max(m, pp)
    }
    if (k < -366) {
        sz -= 847;m = max(m, sz)
    }
    if (o != -1) {
        wnh -= -717;m = max(m, wnh)
    }
    if (e == 26) {
        xbp -= -607;m = max(m, xbp)
    }
    if (y > 788) {
        jyi += -362;m = max(m, jyi)
    }
    if (baj <= -440) {
        wfu += 306;m = max(m, wfu)
    }
    if (pp == -1065) {
        sb += -512;m = max(m, sb)
    }
    if (sqf != -4) {
        jyi -= -190;m = max(m, jyi)
    }
    if (sqf >= -6) {
        k -= 810;m = max(m, k)
    }
    if (fy > 1011) {
        u += 76;m = max(m, u)
    }
    if (c <= 286) {
        sqf += -844;m = max(m, sqf)
    }
    if (k <= -1162) {
        wnh += -326;m = max(m, wnh)
    }
    if (baj == -447) {
        wnh -= -199;m = max(m, wnh)
    }
    if (sqf < -839) {
        k += -828;m = max(m, k)
    }
    if (l > -321) {
        wnh += 288;m = max(m, wnh)
    }
    if (c != 280) {
        o -= 769;m = max(m, o)
    }
    if (rz > 299) {
        pp -= -95;m = max(m, pp)
    }
    if (b <= 982) {
        vke -= -752;m = max(m, vke)
    }
    if (pp == -964) {
        rz += 334;m = max(m, rz)
    }
    if (y >= 797) {
        sqf += 987;m = max(m, sqf)
    }
    if (az > 271) {
        eia += -938;m = max(m, eia)
    }
    if (b > 972) {
        tme -= 849;m = max(m, tme)
    }
    if (c <= 282) {
        vke += 322;m = max(m, vke)
    }
    if (baj >= -446) {
        l += -43;m = max(m, l)
    }
    if (rz <= 641) {
        fy -= 760;m = max(m, fy)
    }
    if (wnh != -80) {
        sqf -= 947;m = max(m, sqf)
    }
    if (eia == -6) {
        e += 267;m = max(m, e)
    }
    if (b >= 975) {
        xbp -= -340;m = max(m, xbp)
    }
    if (wfu != 315) {
        xbp -= 887;m = max(m, xbp)
    }
}
fun two() {
    if (o < -766) {
        az += 911;m = max(m, az)
    }
    if (wnh < -87) {
        sb += 494;m = max(m, sb)
    }
    if (sz <= 8) {
        sqf -= -25;m = max(m, sqf)
    }
    if (xml == 0) {
        fy -= -36;m = max(m, fy)
    }
    if (rz >= 652) {
        ugc -= 429;m = max(m, ugc)
    }
    if (ugc <= 130) {
        sz -= 139;m = max(m, sz)
    }
    if (sz != -141) {
        eia += -762;m = max(m, eia)
    }
    if (vke == 1079) {
        c += 868;m = max(m, c)
    }
    if (pp > -974) {
        l -= 36;m = max(m, l)
    }
    if (wnh != -81) {
        jyi -= -645;m = max(m, jyi)
    }
    if (tme > -840) {
        tme += -361;m = max(m, tme)
    }
    if (eia != -762) {
        sqf += 761;m = max(m, sqf)
    }
    if (xbp != -548) {
        o += 361;m = max(m, o)
    }
    if (pp > -969) {
        pp -= -999;m = max(m, pp)
    }
    if (k <= -1995) {
        sz += -383;m = max(m, sz)
    }
    if (ugc != 132) {
        y -= -940;m = max(m, y)
    }
    if (k < -1989) {
        sqf -= -957;m = max(m, sqf)
    }
    if (e >= 38) {
        tme -= 528;m = max(m, tme)
    }
    if (rz == 643) {
        baj += -803;m = max(m, baj)
    }
    if (sz > -530) {
        y += 799;m = max(m, y)
    }
    if (vke != 1086) {
        fy -= 120;m = max(m, fy)
    }
    if (pp <= 36) {
        wnh += 141;m = max(m, wnh)
    }
    if (wfu == 306) {
        c -= 481;m = max(m, c)
    }
    if (eia < -758) {
        tme += 536;m = max(m, tme)
    }
    if (u <= -174) {
        sz += 954;m = max(m, sz)
    }
    if (y <= 2528) {
        o -= 473;m = max(m, o)
    }
    if (wfu >= 297) {
        l -= 976;m = max(m, l)
    }
    if (baj != -1252) {
        tme += -26;m = max(m, tme)
    }
    if (xml > -7) {
        k -= 786;m = max(m, k)
    }
    if (sqf == 178) {
        az -= 817;m = max(m, az)
    }
    if (k == -2776) {
        c -= 980;m = max(m, c)
    }
    if (tme == -348) {
        baj -= -954;m = max(m, baj)
    }
    if (vke <= 1088) {
        rz += -178;m = max(m, rz)
    }
    if (fy >= 930) {
        jyi += 426;m = max(m, jyi)
    }
    if (wnh >= 63) {
        xbp += -708;m = max(m, xbp)
    }
    if (az > 353) {
        c += 364;m = max(m, c)
    }
    if (xml == 0) {
        l -= -393;m = max(m, l)
    }
    if (l != -983) {
        wfu -= -619;m = max(m, wfu)
    }
    if (xbp == -547) {
        y += -454;m = max(m, y)
    }
    if (jyi >= 642) {
        wnh += 909;m = max(m, wnh)
    }
    if (k > -2777) {
        az += 251;m = max(m, az)
    }
    if (rz >= 470) {
        c += 985;m = max(m, c)
    }
    if (wnh <= 978) {
        e += -810;m = max(m, e)
    }
    if (fy == 934) {
        e += 785;m = max(m, e)
    }
    if (vke >= 1083) {
        wnh -= -872;m = max(m, wnh)
    }
    if (rz > 457) {
        baj -= -827;m = max(m, baj)
    }
    if (sqf > 170) {
        wfu -= -186;m = max(m, wfu)
    }
    if (wnh > 970) {
        sqf += 522;m = max(m, sqf)
    }
    if (wfu <= 489) {
        sb += 307;m = max(m, sb)
    }
    if (sz == 432) {
        ugc -= -765;m = max(m, ugc)
    }
    if (e > 7) {
        wfu += 429;m = max(m, wfu)
    }
    if (jyi <= 653) {
        fy += 730;m = max(m, fy)
    }
    if (sb >= 376) {
        sb -= -265;m = max(m, sb)
    }
    if (c == 1033) {
        b -= -276;m = max(m, b)
    }
    if (k > -2788) {
        u += -99;m = max(m, u)
    }
    if (l >= -986) {
        e += 114;m = max(m, e)
    }
    if (wnh >= 963) {
        vke -= -845;m = max(m, vke)
    }
    if (xbp > -549) {
        c += -263;m = max(m, c)
    }
    if (sz > 425) {
        sz += 905;m = max(m, sz)
    }
    if (o >= -412) {
        baj -= 298;m = max(m, baj)
    }
}
fun three() {
    if (baj == -719) {
        l -= 118;m = max(m, l)
    }
    if (pp >= 31) {
        rz += -472;m = max(m, rz)
    }
    if (fy <= 1670) {
        sb -= 489;m = max(m, sb)
    }
    if (sqf == 178) {
        xbp -= 876;m = max(m, xbp)
    }
    if (o != -407) {
        y -= 504;m = max(m, y)
    }
    if (xbp >= -1427) {
        u += 410;m = max(m, u)
    }
    if (xml != -2) {
        xml -= 95;m = max(m, xml)
    }
    if (xml <= -101) {
        wfu -= 440;m = max(m, wfu)
    }
    if (pp <= 39) {
        sqf -= -270;m = max(m, sqf)
    }
    if (pp >= 34) {
        k += -812;m = max(m, k)
    }
    if (pp > 27) {
        jyi -= -498;m = max(m, jyi)
    }
    if (vke != 1924) {
        jyi -= 848;m = max(m, jyi)
    }
    if (sqf > 440) {
        xbp += 675;m = max(m, xbp)
    }
    if (b <= 981) {
        jyi -= -697;m = max(m, jyi)
    }
    if (baj > -725) {
        jyi -= 172;m = max(m, jyi)
    }
    if (c >= 767) {
        fy += 349;m = max(m, fy)
    }
    if (c >= 771) {
        wnh += -683;m = max(m, wnh)
    }
    if (u <= 144) {
        e += -667;m = max(m, e)
    }
    if (rz != -7) {
        jyi -= -991;m = max(m, jyi)
    }
    if (k < -3586) {
        sb += 547;m = max(m, sb)
    }
    if (jyi != 1669) {
        l += -645;m = max(m, l)
    }
    if (xml > -99) {
        sqf -= 912;m = max(m, sqf)
    }
    if (az >= 354) {
        u += 782;m = max(m, u)
    }
    if (xbp > -755) {
        az -= 752;m = max(m, az)
    }
    if (k >= -3603) {
        y -= 296;m = max(m, y)
    }
    if (fy >= 2007) {
        c -= -816;m = max(m, c)
    }
    if (pp < 43) {
        sqf += 776;m = max(m, sqf)
    }
    if (l != -1742) {
        wfu += 502;m = max(m, wfu)
    }
    if (e != -542) {
        sb -= 228;m = max(m, sb)
    }
    if (k == -3595) {
        fy += 171;m = max(m, fy)
    }
    if (jyi < 1663) {
        wnh -= 998;m = max(m, wnh)
    }
    if (xbp == -748) {
        k -= -762;m = max(m, k)
    }
    if (az == -394) {
        tme -= -660;m = max(m, tme)
    }
    if (vke == 1924) {
        eia += 969;m = max(m, eia)
    }
    if (sqf != 309) {
        l += -324;m = max(m, l)
    }
    if (sz >= 1336) {
        az += -982;m = max(m, az)
    }
    if (tme == 321) {
        rz -= 651;m = max(m, rz)
    }
    if (vke == 1924) {
        xbp -= -196;m = max(m, xbp)
    }
    if (b > 973) {
        eia += 538;m = max(m, eia)
    }
    if (k >= -2842) {
        eia += -326;m = max(m, eia)
    }
    if (eia <= 422) {
        baj -= -47;m = max(m, baj)
    }
    if (eia == 419) {
        jyi += -851;m = max(m, jyi)
    }
    if (xbp >= -553) {
        ugc -= -846;m = max(m, ugc)
    }
    if (wfu < 1424) {
        e += -473;m = max(m, e)
    }
    if (k < -2832) {
        sqf += -401;m = max(m, sqf)
    }
    if (c > 1591) {
        c += 975;m = max(m, c)
    }
    if (xbp == -552) {
        o += -235;m = max(m, o)
    }
    if (k < -2827) {
        xml -= -34;m = max(m, xml)
    }
    if (e >= -1016) {
        xbp += 333;m = max(m, xbp)
    }
    if (sz > 1327) {
        baj -= -74;m = max(m, baj)
    }
    if (vke >= 1921) {
        xml -= -288;m = max(m, xml)
    }
    if (pp <= 42) {
        sqf += -180;m = max(m, sqf)
    }
    if (b != 971) {
        wfu -= 820;m = max(m, wfu)
    }
    if (vke <= 1923) {
        e += -113;m = max(m, e)
    }
    if (rz > -661) {
        jyi += 466;m = max(m, jyi)
    }
    if (pp != 44) {
        eia += -410;m = max(m, eia)
    }
    if (l > -2078) {
        az -= -404;m = max(m, az)
    }
    if (az < -979) {
        wfu += -537;m = max(m, wfu)
    }
    if (pp != 37) {
        xbp += -667;m = max(m, xbp)
    }
    if (b == 979) {
        sz += -774;m = max(m, sz)
    }
    if (b >= 979) {
        sz -= 35;m = max(m, sz)
    }
    if (e < -1020) {
        b += -641;m = max(m, b)
    }
    if (c <= 1588) {
        eia -= 914;m = max(m, eia)
    }
    if (o > -647) {
        pp += -250;m = max(m, pp)
    }
    if (k < -2830) {
        b -= 46;m = max(m, b)
    }
    if (c < 1589) {
        u += 275;m = max(m, u)
    }
    if (az < -971) {
        rz -= -946;m = max(m, rz)
    }
    if (sz == 1337) {
        sqf += -197;m = max(m, sqf)
    }
    if (tme >= 321) {
        sqf -= -853;m = max(m, sqf)
    }
    if (b >= 923) {
        b -= -719;m = max(m, b)
    }
    if (e == -1015) {
        baj -= 614;m = max(m, baj)
    }
    if (u < 1195) {
        o += -224;m = max(m, o)
    }
    if (vke <= 1925) {
        sqf -= 130;m = max(m, sqf)
    }
    if (xml >= 224) {
        eia += 388;m = max(m, eia)
    }
    if (pp > -218) {
        xbp -= 561;m = max(m, xbp)
    }
    if (k >= -2841) {
        o -= -959;m = max(m, o)
    }
    if (az >= -972) {
        tme += -89;m = max(m, tme)
    }
    if (b >= 1652) {
        xml -= 745;m = max(m, xml)
    }
    if (xml <= 235) {
        baj -= 649;m = max(m, baj)
    }
    if (o <= 101) {
        wfu += -762;m = max(m, wfu)
    }
    if (jyi >= 1277) {
        l += 424;m = max(m, l)
    }
    if (o == 101) {
        rz -= 685;m = max(m, rz)
    }
    if (baj >= -1868) {
        u -= -49;m = max(m, u)
    }
    if (eia >= -517) {
        u -= -495;m = max(m, u)
    }
    if (y > 1273) {
        az += -450;m = max(m, az)
    }
    if (y == 1283) {
        sz -= -860;m = max(m, sz)
    }
    if (xml >= 226) {
        wnh += -896;m = max(m, wnh)
    }
    if (tme >= 229) {
        eia -= -319;m = max(m, eia)
    }
    if (o == 92) {
        vke += -535;m = max(m, vke)
    }
    if (fy > 2190) {
        vke -= 10;m = max(m, vke)
    }
    if (l == -1646) {
        b -= -683;m = max(m, b)
    }
    if (jyi > 1274) {
        tme -= -522;m = max(m, tme)
    }
    if (wnh < -604) {
        tme += -211;m = max(m, tme)
    }
    if (rz <= 285) {
        u -= -383;m = max(m, u)
    }
    if (u >= 1732) {
        baj += 928;m = max(m, baj)
    }
    if (y <= 1282) {
        eia += -286;m = max(m, eia)
    }
    if (ugc <= 1747) {
        pp -= -422;m = max(m, pp)
    }
    if (o != 96) {
        e -= -123;m = max(m, e)
    }
    if (wfu == -159) {
        u -= -163;m = max(m, u)
    }
    if (rz <= 289) {
        y -= 802;m = max(m, y)
    }
    if (eia >= -199) {
        o -= -992;m = max(m, o)
    }
    if (e < -887) {
        sqf -= 275;m = max(m, sqf)
    }
    if (e < -889) {
        az += 801;m = max(m, az)
    }
    if (c < 1595) {
        pp += 893;m = max(m, pp)
    }
    if (o < 1094) {
        sz += 913;m = max(m, sz)
    }
    if (k <= -2833) {
        wfu += 870;m = max(m, wfu)
    }
    if (wnh < -609) {
        tme += -203;m = max(m, tme)
    }
    if (xml < 228) {
        eia -= 431;m = max(m, eia)
    }
    if (sz == 3110) {
        sqf -= -722;m = max(m, sqf)
    }
    if (fy > 2175) {
        xml += -527;m = max(m, xml)
    }
    if (fy < 2176) {
        sqf -= -314;m = max(m, sqf)
    }
    if (l < -1636) {
        baj += -425;m = max(m, baj)
    }
    if (k >= -2838) {
        rz -= -774;m = max(m, rz)
    }
    if (pp != 1097) {
        jyi += -797;m = max(m, jyi)
    }
    if (o < 1091) {
        u += 856;m = max(m, u)
    }
    if (o > 1089) {
        ugc -= 29;m = max(m, ugc)
    }
    if (rz <= 1070) {
        wnh -= 629;m = max(m, wnh)
    }
    if (fy != 2184) {
        rz -= 919;m = max(m, rz)
    }
    if (fy >= 2178) {
        wfu -= -789;m = max(m, wfu)
    }
    if (wfu < 1491) {
        o += 229;m = max(m, o)
    }
    if (sb != 432) {
        wnh += -873;m = max(m, wnh)
    }
    if (rz != 1062) {
        wfu += -29;m = max(m, wfu)
    }
    if (ugc == 1739) {
        pp -= 542;m = max(m, pp)
    }
    if (l <= -1638) {
        tme -= -321;m = max(m, tme)
    }
    if (b < 2340) {
        baj -= 515;m = max(m, baj)
    }
    if (sqf >= 701) {
        k -= 976;m = max(m, k)
    }
    if (eia >= -631) {
        e -= -940;m = max(m, e)
    }
    if (l <= -1644) {
        b += 173;m = max(m, b)
    }
    if (sb < 439) {
        e += 649;m = max(m, e)
    }
    if (c > 1579) {
        ugc += 598;m = max(m, ugc)
    }
    if (u == 2755) {
        baj -= 920;m = max(m, baj)
    }
    if (eia < -622) {
        sz += 165;m = max(m, sz)
    }
    if (fy != 2181) {
        c += -461;m = max(m, c)
    }
    if (pp == 558) {
        sqf -= -574;m = max(m, sqf)
    }
    if (o <= 1089) {
        jyi += -511;m = max(m, jyi)
    }
    if (pp == 558) {
        k -= -876;m = max(m, k)
    }
    if (wnh < -1234) {
        l -= -830;m = max(m, l)
    }
    if (pp < 550) {
        az += -524;m = max(m, az)
    }
    if (vke < 1383) {
        y += 141;m = max(m, y)
    }
    if (rz <= 1067) {
        tme += 365;m = max(m, tme)
    }
    if (e >= 696) {
        u += 33;m = max(m, u)
    }
    if (sz == 3275) {
        sqf += -241;m = max(m, sqf)
    }
    if (baj == -2793) {
        k -= 918;m = max(m, k)
    }
    if (y > 471) {
        jyi += 82;m = max(m, jyi)
    }
    if (k >= -3857) {
        sb -= 730;m = max(m, sb)
    }
    if (wnh <= -1237) {
        u -= -690;m = max(m, u)
    }
    if (e >= 695) {
        jyi -= 962;m = max(m, jyi)
    }
    if (wnh <= -1238) {
        xml -= -513;m = max(m, xml)
    }
    if (sz >= 3285) {
        sqf += -77;m = max(m, sqf)
    }
    if (az > -624) {
        wfu += 697;m = max(m, wfu)
    }
    if (eia < -621) {
        l -= 200;m = max(m, l)
    }
    if (k < -3846) {
        l += 258;m = max(m, l)
    }
    if (b <= 2513) {
        wnh -= -578;m = max(m, wnh)
    }
    if (wnh < -659) {
        l += -871;m = max(m, l)
    }
    if (b < 2496) {
        pp -= 916;m = max(m, pp)
    }
    if (fy <= 2189) {
        eia += -445;m = max(m, eia)
    }
    if (o >= 1077) {
        b -= -759;m = max(m, b)
    }
    if (xml != 219) {
        ugc += -582;m = max(m, ugc)
    }
    if (sz < 3280) {
        vke -= 915;m = max(m, vke)
    }
    if (sz != 3271) {
        e -= 752;m = max(m, e)
    }
    if (pp > 550) {
        k += 447;m = max(m, k)
    }
    if (jyi > -910) {
        wnh += -272;m = max(m, wnh)
    }
    if (xml >= 209) {
        b += 762;m = max(m, b)
    }
    if (eia < -1070) {
        vke += 33;m = max(m, vke)
    }
    if (vke >= 501) {
        wnh -= -943;m = max(m, wnh)
    }
    if (wnh != 4) {
        y -= -627;m = max(m, y)
    }
    if (eia >= -1080) {
        k -= -240;m = max(m, k)
    }
    if (eia <= -1069) {
        tme += -731;m = max(m, tme)
    }
    if (pp < 567) {
        y += 138;m = max(m, y)
    }
    if (u < 3480) {
        wfu -= -839;m = max(m, wfu)
    }
    if (az >= -624) {
        sz -= -268;m = max(m, sz)
    }
    if (l > -1629) {
        tme += 413;m = max(m, tme)
    }
    if (l == -1626) {
        vke += 699;m = max(m, vke)
    }
    if (fy != 2179) {
        pp -= 962;m = max(m, pp)
    }
    if (eia != -1068) {
        pp += -972;m = max(m, pp)
    }
    if (xbp < -1445) {
        e -= -223;m = max(m, e)
    }
    if (xbp > -1440) {
        pp += -559;m = max(m, pp)
    }
    if (sb <= -299) {
        pp += -869;m = max(m, pp)
    }
    if (sz == 3543) {
        wfu -= 229;m = max(m, wfu)
    }
    if (fy != 2189) {
        az -= -885;m = max(m, az)
    }
    if (wfu >= 2806) {
        tme += -542;m = max(m, tme)
    }
    if (e >= 172) {
        sqf -= -797;m = max(m, sqf)
    }
    if (c >= 1119) {
        l += -159;m = max(m, l)
    }
    if (e > 158) {
        baj -= 602;m = max(m, baj)
    }
    if (sb < -295) {
        sb -= -546;m = max(m, sb)
    }
    if (k != -3171) {
        ugc += -531;m = max(m, ugc)
    }
    if (sqf > 1033) {
        xbp += -800;m = max(m, xbp)
    }
    if (tme >= -255) {
        eia -= 281;m = max(m, eia)
    }
    if (tme != -247) {
        b += -997;m = max(m, b)
    }
    if (xml >= 216) {
        k += 140;m = max(m, k)
    }
    if (pp > -1377) {
        pp += -83;m = max(m, pp)
    }
    if (l <= -1781) {
        sb += -697;m = max(m, sb)
    }
    if (wnh < 19) {
        baj += 101;m = max(m, baj)
    }
    if (wnh < 17) {
        b += -330;m = max(m, b)
    }
    if (ugc > 1219) {
        baj -= -602;m = max(m, baj)
    }
    if (tme == -247) {
        fy -= 642;m = max(m, fy)
    }
    if (c >= 1120) {
        e += 212;m = max(m, e)
    }
    if (c > 1122) {
        sqf -= 693;m = max(m, sqf)
    }
    if (az <= 265) {
        tme -= 277;m = max(m, tme)
    }
    if (ugc >= 1222) {
        k -= 430;m = max(m, k)
    }
    if (wfu >= 2806) {
        rz += 182;m = max(m, rz)
    }
    if (wfu == 2807) {
        sb += 158;m = max(m, sb)
    }
    if (wfu == 2807) {
        wfu += -223;m = max(m, wfu)
    }
    if (ugc != 1224) {
        pp -= -734;m = max(m, pp)
    }
    if (c != 1124) {
        rz -= -809;m = max(m, rz)
    }
    if (y < 1247) {
        vke -= -963;m = max(m, vke)
    }
    if (l != -1791) {
        sqf -= -654;m = max(m, sqf)
    }
    if (o < 1090) {
        az -= -939;m = max(m, az)
    }
    if (vke == 1470) {
        eia += 753;m = max(m, eia)
    }
    if (u > 3473) {
        fy -= 494;m = max(m, fy)
    }
    if (ugc > 1217) {
        jyi += -739;m = max(m, jyi)
    }
    if (e != 380) {
        jyi += 216;m = max(m, jyi)
    }
    if (ugc > 1222) {
        l += -95;m = max(m, l)
    }
    if (baj > -2695) {
        pp -= 400;m = max(m, pp)
    }
    if (k < -3603) {
        o -= -915;m = max(m, o)
    }
    if (c <= 1128) {
        c -= 810;m = max(m, c)
    }
    if (k >= -3598) {
        fy -= 33;m = max(m, fy)
    }
    if (sz <= 3540) {
        c += -193;m = max(m, c)
    }
    if (l > -1885) {
        ugc += -415;m = max(m, ugc)
    }
    if (baj != -2692) {
        c += 12;m = max(m, c)
    }
    if (c != 309) {
        eia -= 376;m = max(m, eia)
    }
    if (xbp != -2255) {
        jyi += 168;m = max(m, jyi)
    }
    if (sz != 3553) {
        u -= 814;m = max(m, u)
    }
    if (xbp != -2247) {
        wfu -= 247;m = max(m, wfu)
    }
    if (eia <= -970) {
        xml -= -716;m = max(m, xml)
    }
    if (baj <= -2689) {
        sqf -= -985;m = max(m, sqf)
    }
    if (pp != -1867) {
        sz += 280;m = max(m, sz)
    }
    if (jyi <= -1475) {
        tme -= 277;m = max(m, tme)
    }
    if (l == -1883) {
        pp -= 122;m = max(m, pp)
    }
    if (sz != 3818) {
        tme -= -329;m = max(m, tme)
    }
    if (sb == -291) {
        eia -= 980;m = max(m, eia)
    }
    if (b > 3696) {
        jyi += 373;m = max(m, jyi)
    }
    if (baj < -2688) {
        y += -203;m = max(m, y)
    }
    if (u == 2664) {
        ugc += 263;m = max(m, ugc)
    }
    if (xbp >= -2250) {
        eia += -278;m = max(m, eia)
    }
    if (xbp <= -2246) {
        ugc -= 654;m = max(m, ugc)
    }
    if (rz == 2053) {
        fy += 814;m = max(m, fy)
    }
    if (y != 1034) {
        wnh += 709;m = max(m, wnh)
    }
    if (tme > -476) {
        e -= 939;m = max(m, e)
    }
    if (az > 1193) {
        wfu += -792;m = max(m, wfu)
    }
    if (baj <= -2689) {
        u += -506;m = max(m, u)
    }
    if (vke == 1480) {
        b += 350;m = max(m, b)
    }
    if (o > 1074) {
        o += 938;m = max(m, o)
    }
    if (rz == 2053) {
        jyi += -219;m = max(m, jyi)
    }
    if (u >= 2162) {
        wfu -= 349;m = max(m, wfu)
    }
    if (o > 2025) {
        u -= -97;m = max(m, u)
    }
    if (vke != 1471) {
        pp += -211;m = max(m, pp)
    }
    if (b < 3699) {
        wfu -= -194;m = max(m, wfu)
    }
    if (sqf <= 1980) {
        l -= 96;m = max(m, l)
    }
    if (az >= 1198) {
        vke -= 179;m = max(m, vke)
    }
    if (wnh != 719) {
        vke -= -317;m = max(m, vke)
    }
    if (baj < -2689) {
        tme += 606;m = max(m, tme)
    }
    if (fy > 1829) {
        k -= 148;m = max(m, k)
    }
    if (y >= 1041) {
        jyi -= 294;m = max(m, jyi)
    }
    if (xbp > -2245) {
        wfu += 963;m = max(m, wfu)
    }
    if (o == 2022) {
        sb -= -932;m = max(m, sb)
    }
    if (c <= 316) {
        wnh += -640;m = max(m, wnh)
    }
    if (o != 2021) {
        rz += 90;m = max(m, rz)
    }
    if (rz != 2134) {
        tme += 466;m = max(m, tme)
    }
    if (k < -3586) {
        l -= -771;m = max(m, l)
    }
    if (e <= -551) {
        vke -= 91;m = max(m, vke)
    }
    if (b < 3705) {
        fy -= -312;m = max(m, fy)
    }
    if (wnh >= 71) {
        vke -= -529;m = max(m, vke)
    }
    if (xml < 937) {
        b -= -595;m = max(m, b)
    }
    if (b != 4293) {
        tme -= 540;m = max(m, tme)
    }
    if (baj <= -2702) {
        o -= 502;m = max(m, o)
    }
    if (jyi > -1991) {
        tme += -270;m = max(m, tme)
    }
    if (u >= 2157) {
        b -= -298;m = max(m, b)
    }
    if (sz <= 3813) {
        c -= -599;m = max(m, c)
    }
    if (b > 4588) {
        y -= -909;m = max(m, y)
    }
    if (pp != -2201) {
        y += 477;m = max(m, y)
    }
    if (u < 2163) {
        xbp += 201;m = max(m, xbp)
    }
    if (sb >= 636) {
        jyi += 101;m = max(m, jyi)
    }
    if (b == 4589) {
        l -= 75;m = max(m, l)
    }
    if (e <= -558) {
        e += -15;m = max(m, e)
    }
    if (sb != 638) {
        fy += -471;m = max(m, fy)
    }
    if (u < 2167) {
        jyi += 715;m = max(m, jyi)
    }
    if (az == 1203) {
        pp -= 364;m = max(m, pp)
    }
    if (sqf >= 1988) {
        az -= -740;m = max(m, az)
    }
    if (wnh < 75) {
        tme += 772;m = max(m, tme)
    }
    if (u > 2156) {
        k -= 634;m = max(m, k)
    }
    if (u <= 2162) {
        eia -= 141;m = max(m, eia)
    }
    if (l < -1184) {
        baj -= 776;m = max(m, baj)
    }
    if (fy <= 1677) {
        wnh -= -920;m = max(m, wnh)
    }
    if (c <= 320) {
        xbp -= 169;m = max(m, xbp)
    }
    if (vke < 1736) {
        sqf += -193;m = max(m, sqf)
    }
    if (vke > 1724) {
        fy -= 96;m = max(m, fy)
    }
    if (az < 1206) {
        u += -282;m = max(m, u)
    }
    if (wnh != 1000) {
        xml += -395;m = max(m, xml)
    }
    if (k <= -4233) {
        az += -492;m = max(m, az)
    }
    if (u < 1883) {
        sz += -210;m = max(m, sz)
    }
    if (eia == -2380) {
        c += -584;m = max(m, c)
    }
    if (wnh >= 994) {
        pp += -772;m = max(m, pp)
    }
    if (xml >= 528) {
        ugc += -946;m = max(m, ugc)
    }
    if (rz < 2147) {
        l -= -848;m = max(m, l)
    }
    if (sqf > 1780) {
        o -= -570;m = max(m, o)
    }
    if (e < -564) {
        ugc += -849;m = max(m, ugc)
    }
    if (pp > -3332) {
        sb -= -327;m = max(m, sb)
    }
    if (u != 1869) {
        sqf -= -937;m = max(m, sqf)
    }
    if (o >= 2592) {
        xbp += 765;m = max(m, xbp)
    }
    if (fy > 1574) {
        ugc += -2;m = max(m, ugc)
    }
    if (y < 2432) {
        xml += -976;m = max(m, xml)
    }
    if (b > 4587) {
        o += 762;m = max(m, o)
    }
    if (tme == -210) {
        xml += 384;m = max(m, xml)
    }
    if (wnh < 1005) {
        u += -612;m = max(m, u)
    }
    if (e != -582) {
        u += -229;m = max(m, u)
    }
    if (wfu <= 1989) {
        eia -= 814;m = max(m, eia)
    }
    if (sb > 964) {
        fy += -41;m = max(m, fy)
    }
    if (wnh != 1004) {
        fy -= -487;m = max(m, fy)
    }
    if (jyi < -1164) {
        xml -= -44;m = max(m, xml)
    }
    if (sqf == 2727) {
        k -= 107;m = max(m, k)
    }
    if (rz < 2141) {
        sz += -819;m = max(m, sz)
    }
    if (wnh < 1009) {
        k += 862;m = max(m, k)
    }
    if (b == 4592) {
        vke += -935;m = max(m, vke)
    }
    if (y != 2429) {
        k -= -547;m = max(m, k)
    }
    if (l >= -331) {
        k -= -804;m = max(m, k)
    }
    if (jyi >= -1178) {
        o -= -869;m = max(m, o)
    }
    if (wfu != 1986) {
        fy += -527;m = max(m, fy)
    }
    if (jyi <= -1175) {
        vke += -900;m = max(m, vke)
    }
    if (pp < -3322) {
        ugc += 878;m = max(m, ugc)
    }
    if (eia > -3192) {
        sz += 96;m = max(m, sz)
    }
    if (l != -346) {
        tme += -888;m = max(m, tme)
    }
    if (e == -574) {
        rz -= -879;m = max(m, rz)
    }
}
fun four() {
    if (wfu <= 1983) {
        y += -89;m = max(m, y)
    }
    if (xml <= -21) {
        u -= 106;m = max(m, u)
    }
    if (y <= 2423) {
        az -= 724;m = max(m, az)
    }
    if (wnh == 999) {
        sb += -951;m = max(m, sb)
    }
    if (xbp == -1450) {
        wnh += -583;m = max(m, wnh)
    }
    if (c >= 311) {
        sz -= -906;m = max(m, sz)
    }
    if (b <= 4583) {
        az += -192;m = max(m, az)
    }
    if (vke <= 1728) {
        l -= 307;m = max(m, l)
    }
    if (xbp >= -1455) {
        xbp -= 553;m = max(m, xbp)
    }
    if (tme > -1100) {
        u += 581;m = max(m, u)
    }
    if (xml >= -22) {
        e += 993;m = max(m, e)
    }
    if (y != 2422) {
        baj -= 484;m = max(m, baj)
    }
    if (xbp <= -2002) {
        u -= 797;m = max(m, u)
    }
    if (sqf > 2729) {
        xml -= -448;m = max(m, xml)
    }
    if (wnh <= 413) {
        l -= -97;m = max(m, l)
    }
    if (baj <= -3959) {
        c -= -211;m = max(m, c)
    }
    if (xbp > -2008) {
        k -= 925;m = max(m, k)
    }
    if (c == 316) {
        wnh += 198;m = max(m, wnh)
    }
    if (l <= -331) {
        l += 22;m = max(m, l)
    }
    if (xbp <= -2006) {
        jyi += -293;m = max(m, jyi)
    }
    if (y <= 2432) {
        e -= 271;m = max(m, e)
    }
    if (pp > -3327) {
        pp -= 309;m = max(m, pp)
    }
    if (jyi <= -1181) {
        c -= 645;m = max(m, c)
    }
    if (y <= 2428) {
        l += 706;m = max(m, l)
    }
    if (c >= 307) {
        xbp += 477;m = max(m, xbp)
    }
    if (u != 811) {
        l += -998;m = max(m, l)
    }
    if (az != 1193) {
        xbp += 879;m = max(m, xbp)
    }
    if (wfu != 1985) {
        k -= 954;m = max(m, k)
    }
    if (jyi == -1173) {
        sb += -627;m = max(m, sb)
    }
    if (wnh != 614) {
        vke += 766;m = max(m, vke)
    }
    if (wnh != 616) {
        pp += -177;m = max(m, pp)
    }
    if (wfu == 1986) {
        y += -426;m = max(m, y)
    }
    if (wfu >= 1979) {
        wnh += -932;m = max(m, wnh)
    }
    if (u != 821) {
        sqf += 328;m = max(m, sqf)
    }
    if (u < 827) {
        l -= -995;m = max(m, l)
    }
    if (sb < -606) {
        xml += 856;m = max(m, xml)
    }
    if (b >= 4586) {
        tme -= 985;m = max(m, tme)
    }
    if (wnh > -320) {
        y -= 992;m = max(m, y)
    }
    if (y == 1011) {
        baj -= -833;m = max(m, baj)
    }
    if (vke <= 1735) {
        fy -= -733;m = max(m, fy)
    }
    if (ugc <= -491) {
        xml += 471;m = max(m, xml)
    }
    if (tme < -2082) {
        c -= -970;m = max(m, c)
    }
    if (sqf != 3051) {
        eia += -404;m = max(m, eia)
    }
    if (rz < 3025) {
        sqf += 646;m = max(m, sqf)
    }
    if (vke >= 1729) {
        jyi -= 997;m = max(m, jyi)
    }
    if (pp <= -3502) {
        c += 114;m = max(m, c)
    }
    if (l != -322) {
        b += 385;m = max(m, b)
    }
    if (rz < 3016) {
        ugc += -76;m = max(m, ugc)
    }
    if (wfu == 1986) {
        sqf -= -180;m = max(m, sqf)
    }
    if (jyi == -2170) {
        jyi += 876;m = max(m, jyi)
    }
    if (c == 1400) {
        jyi += -663;m = max(m, jyi)
    }
    if (sb >= -612) {
        k -= -530;m = max(m, k)
    }
    if (rz < 3032) {
        jyi += 790;m = max(m, jyi)
    }
    if (wnh >= -319) {
        vke -= -964;m = max(m, vke)
    }
    if (ugc > -505) {
        k += 165;m = max(m, k)
    }
    if (vke > 2684) {
        xml -= -928;m = max(m, xml)
    }
    if (e < 155) {
        pp += 739;m = max(m, pp)
    }
    if (baj < -3110) {
        sz += 711;m = max(m, sz)
    }
    if (y < 1020) {
        o += -420;m = max(m, o)
    }
    if (jyi <= -1162) {
        xbp += 362;m = max(m, xbp)
    }
    if (az > 1201) {
        wnh += -228;m = max(m, wnh)
    }
    if (xbp < -275) {
        jyi += 931;m = max(m, jyi)
    }
    if (wfu > 1983) {
        rz += 650;m = max(m, rz)
    }
    if (k > -4667) {
        rz += 136;m = max(m, rz)
    }
    if (xml != 2236) {
        c -= -923;m = max(m, c)
    }
    if (u == 819) {
        az -= -127;m = max(m, az)
    }
    if (baj <= -3115) {
        fy -= 717;m = max(m, fy)
    }
    if (o <= 3807) {
        eia += 14;m = max(m, eia)
    }
    if (sqf >= 3879) {
        tme += 928;m = max(m, tme)
    }
    if (e <= 155) {
        vke += -148;m = max(m, vke)
    }
    if (az <= 1337) {
        tme += 959;m = max(m, tme)
    }
    if (e <= 139) {
        c -= 306;m = max(m, c)
    }
    if (b == 4974) {
        vke += -622;m = max(m, vke)
    }
    if (sb >= -611) {
        c += -818;m = max(m, c)
    }
    if (l >= -328) {
        az += -107;m = max(m, az)
    }
    if (u <= 826) {
        l -= 42;m = max(m, l)
    }
    if (ugc == -508) {
        fy += -175;m = max(m, fy)
    }
    if (o <= 3806) {
        e += 208;m = max(m, e)
    }
    if (c != 1515) {
        fy += 239;m = max(m, fy)
    }
    if (wnh != -542) {
        sz -= -678;m = max(m, sz)
    }
    if (e < 348) {
        sz -= 660;m = max(m, sz)
    }
    if (wfu > 1982) {
        az -= 636;m = max(m, az)
    }
    if (k > -4658) {
        tme += -537;m = max(m, tme)
    }
    if (eia <= -3580) {
        sb -= 977;m = max(m, sb)
    }
    if (b < 4982) {
        u += -171;m = max(m, u)
    }
    if (baj == -3119) {
        wnh += 141;m = max(m, wnh)
    }
    if (l == -362) {
        rz += -648;m = max(m, rz)
    }
    if (eia != -3579) {
        eia -= 910;m = max(m, eia)
    }
    if (l != -355) {
        sz -= -202;m = max(m, sz)
    }
    if (sz <= 6214) {
        fy -= -235;m = max(m, fy)
    }
    if (eia == -4491) {
        vke += 635;m = max(m, vke)
    }
    if (b < 4977) {
        eia += -472;m = max(m, eia)
    }
    if (tme < -729) {
        k -= -936;m = max(m, k)
    }
    if (eia <= -4958) {
        u -= -793;m = max(m, u)
    }
    if (jyi == -236) {
        jyi += -273;m = max(m, jyi)
    }
    if (u == 1441) {
        xml -= 101;m = max(m, xml)
    }
    if (sz > 6205) {
        baj += 553;m = max(m, baj)
    }
    if (wfu <= 1995) {
        c -= -315;m = max(m, c)
    }
    if (l < -356) {
        e -= 504;m = max(m, e)
    }
    if (eia > -4968) {
        u -= -563;m = max(m, u)
    }
    if (pp != -2768) {
        wnh += -81;m = max(m, wnh)
    }
    if (xml == 2140) {
        rz -= -784;m = max(m, rz)
    }
    if (l != -366) {
        xbp -= 113;m = max(m, xbp)
    }
    if (vke > 2554) {
        b -= -545;m = max(m, b)
    }
    if (e <= -151) {
        c -= -978;m = max(m, c)
    }
    if (eia == -4963) {
        baj -= -782;m = max(m, baj)
    }
    if (pp < -2765) {
        wnh -= 524;m = max(m, wnh)
    }
    if (sb >= -1587) {
        sz -= 272;m = max(m, sz)
    }
    if (jyi > -519) {
        baj += -501;m = max(m, baj)
    }
    if (y < 1013) {
        xml -= -246;m = max(m, xml)
    }
    if (xml < 2377) {
        az += -607;m = max(m, az)
    }
    if (pp >= -2769) {
        az -= -25;m = max(m, az)
    }
    if (eia < -4962) {
        pp -= -488;m = max(m, pp)
    }
    if (xml > 2382) {
        b += -551;m = max(m, b)
    }
    if (az == 612) {
        sz += 318;m = max(m, sz)
    }
    if (pp == -2278) {
        xml -= -645;m = max(m, xml)
    }
    if (e < -140) {
        u -= 681;m = max(m, u)
    }
    if (sb >= -1588) {
        b += -690;m = max(m, b)
    }
    if (xml < 3034) {
        baj -= 927;m = max(m, baj)
    }
    if (wnh >= -1015) {
        e -= 645;m = max(m, e)
    }
    if (xbp <= -394) {
        sqf -= 621;m = max(m, sqf)
    }
    if (fy <= 2513) {
        rz += 963;m = max(m, rz)
    }
    if (sz == 6252) {
        tme -= -163;m = max(m, tme)
    }
    if (baj > -3220) {
        vke += -132;m = max(m, vke)
    }
    if (sqf <= 3265) {
        tme += -151;m = max(m, tme)
    }
    if (fy >= 2505) {
        az -= 47;m = max(m, az)
    }
    if (vke > 2421) {
        u += -185;m = max(m, u)
    }
    if (sb != -1582) {
        jyi += 785;m = max(m, jyi)
    }
    if (e > -795) {
        k -= -427;m = max(m, k)
    }
    if (xbp != -395) {
        sz -= 652;m = max(m, sz)
    }
    if (eia >= -4967) {
        eia += -102;m = max(m, eia)
    }
    if (wfu < 1990) {
        b += -860;m = max(m, b)
    }
    if (b > 3416) {
        rz -= -501;m = max(m, rz)
    }
    if (pp == -2278) {
        y -= -772;m = max(m, y)
    }
    if (y >= 1777) {
        sz -= -982;m = max(m, sz)
    }
    if (fy != 2510) {
        rz += 874;m = max(m, rz)
    }
    if (rz < 5410) {
        fy += 965;m = max(m, fy)
    }
    if (c < 1830) {
        sb += 303;m = max(m, sb)
    }
    if (wnh == -1010) {
        pp += 658;m = max(m, pp)
    }
    if (xml > 3029) {
        fy += 206;m = max(m, fy)
    }
    if (az > 558) {
        fy -= 60;m = max(m, fy)
    }
    if (jyi != 276) {
        xbp += -260;m = max(m, xbp)
    }
    if (e >= -799) {
        xbp += 398;m = max(m, xbp)
    }
    if (xml < 3036) {
        sb -= -532;m = max(m, sb)
    }
    if (az != 565) {
        wfu += -63;m = max(m, wfu)
    }
    if (xbp <= 0) {
        ugc += -898;m = max(m, ugc)
    }
    if (e < -789) {
        rz -= 875;m = max(m, rz)
    }
    if (az < 558) {
        l += -517;m = max(m, l)
    }
    if (u == 1138) {
        wnh -= 936;m = max(m, wnh)
    }
    if (pp >= -1623) {
        k += 665;m = max(m, k)
    }
    if (rz == 4533) {
        xbp += 200;m = max(m, xbp)
    }
    if (b < 3428) {
        k += 521;m = max(m, k)
    }
    if (baj >= -3215) {
        baj -= -634;m = max(m, baj)
    }
    if (rz > 4523) {
        l -= 265;m = max(m, l)
    }
    if (l >= -624) {
        sb -= -725;m = max(m, sb)
    }
    if (wnh < -1944) {
        eia += 959;m = max(m, eia)
    }
    if (b == 3418) {
        rz -= 721;m = max(m, rz)
    }
    if (sb <= -746) {
        sqf += -659;m = max(m, sqf)
    }
    if (sb == -752) {
        pp -= -30;m = max(m, pp)
    }
    if (b <= 3424) {
        sqf -= 938;m = max(m, sqf)
    }
    if (k < -2108) {
        k += 166;m = max(m, k)
    }
    if (o >= 3799) {
        u -= 392;m = max(m, u)
    }
    if (tme == -721) {
        l += -784;m = max(m, l)
    }
    if (pp >= -1596) {
        sb -= 217;m = max(m, sb)
    }
    if (b == 3418) {
        wnh -= -926;m = max(m, wnh)
    }
    if (rz < 3821) {
        u += -7;m = max(m, u)
    }
    if (baj >= -2582) {
        sb -= -197;m = max(m, sb)
    }
    if (vke > 2419) {
        u += -449;m = max(m, u)
    }
    if (c <= 1827) {
        c -= 688;m = max(m, c)
    }
    if (sz < 6578) {
        fy -= 53;m = max(m, fy)
    }
    if (jyi < 284) {
        c -= 687;m = max(m, c)
    }
    if (tme == -721) {
        pp -= -391;m = max(m, pp)
    }
    if (ugc <= -1392) {
        pp -= 695;m = max(m, pp)
    }
    if (e > -803) {
        xbp -= 132;m = max(m, xbp)
    }
    if (eia > -4111) {
        e -= 215;m = max(m, e)
    }
    if (eia > -4112) {
        sb -= 602;m = max(m, sb)
    }
    if (baj == -2578) {
        xbp += -885;m = max(m, xbp)
    }
    if (sqf >= 1655) {
        vke -= -709;m = max(m, vke)
    }
    if (xbp > -819) {
        az -= -378;m = max(m, az)
    }
    if (sb != -1368) {
        vke -= -263;m = max(m, vke)
    }
    if (l >= -1413) {
        baj -= -783;m = max(m, baj)
    }
    if (c != 442) {
        k += 792;m = max(m, k)
    }
    if (sb < -1368) {
        c += -31;m = max(m, c)
    }
    if (rz < 3821) {
        sb -= 904;m = max(m, sb)
    }
    if (eia == -4106) {
        fy += 167;m = max(m, fy)
    }
    if (e > -999) {
        xml += -764;m = max(m, xml)
    }
    if (baj <= -1794) {
        wnh -= -530;m = max(m, wnh)
    }
    if (e >= -998) {
        jyi -= -748;m = max(m, jyi)
    }
    if (e >= -1014) {
        u += -653;m = max(m, u)
    }
    if (l <= -1403) {
        tme += -791;m = max(m, tme)
    }
    if (e == -1008) {
        fy -= -800;m = max(m, fy)
    }
    if (k < -1313) {
        az -= 783;m = max(m, az)
    }
    if (pp >= -1900) {
        e += -553;m = max(m, e)
    }
    if (baj < -1802) {
        wfu -= -472;m = max(m, wfu)
    }
    if (b >= 3427) {
        xbp -= -53;m = max(m, xbp)
    }
    if (xbp < -816) {
        fy -= 174;m = max(m, fy)
    }
    if (vke < 3406) {
        o -= 581;m = max(m, o)
    }
    if (y <= 1785) {
        k += -458;m = max(m, k)
    }
    if (az == 160) {
        az += 59;m = max(m, az)
    }
    if (wfu > 1978) {
        tme += 291;m = max(m, tme)
    }
    if (wfu >= 1989) {
        pp -= -933;m = max(m, pp)
    }
    if (l >= -1415) {
        jyi -= -982;m = max(m, jyi)
    }
    if (jyi == 1258) {
        pp += 403;m = max(m, pp)
    }
    if (pp >= -1491) {
        l += -149;m = max(m, l)
    }
    if (sb < -2275) {
        fy -= -663;m = max(m, fy)
    }
    if (vke >= 3401) {
        vke += -866;m = max(m, vke)
    }
    if (l < -1553) {
        sqf -= -397;m = max(m, sqf)
    }
    if (wfu > 1981) {
        xbp += 1000;m = max(m, xbp)
    }
    if (wfu >= 1979) {
        wnh += -188;m = max(m, wnh)
    }
    if (l > -1569) {
        l -= -260;m = max(m, l)
    }
    if (xml <= 3033) {
        baj += 109;m = max(m, baj)
    }
    if (rz < 3820) {
        wfu -= -98;m = max(m, wfu)
    }
    if (c < 417) {
        az += -676;m = max(m, az)
    }
    if (wfu != 2083) {
        e -= -459;m = max(m, e)
    }
    if (sz >= 6586) {
        sb += -556;m = max(m, sb)
    }
    if (pp != -1490) {
        l -= -104;m = max(m, l)
    }
    if (e < -1096) {
        jyi -= 835;m = max(m, jyi)
    }
    if (wfu < 2094) {
        u += 63;m = max(m, u)
    }
    if (ugc > -1405) {
        k -= 531;m = max(m, k)
    }
    if (xbp < 184) {
        wfu -= 438;m = max(m, wfu)
    }
    if (sz < 6589) {
        wnh -= 304;m = max(m, wnh)
    }
    if (ugc <= -1391) {
        xml += -278;m = max(m, xml)
    }
    if (y == 1783) {
        pp += -25;m = max(m, pp)
    }
    if (xml == 2753) {
        k -= 179;m = max(m, k)
    }
    if (u > -303) {
        wfu -= 711;m = max(m, wfu)
    }
    if (wfu >= 931) {
        eia -= 575;m = max(m, eia)
    }
    if (ugc <= -1398) {
        sb += -1;m = max(m, sb)
    }
    if (xml >= 2751) {
        pp -= -948;m = max(m, pp)
    }
    if (pp >= -558) {
        xbp -= -441;m = max(m, xbp)
    }
    if (fy == 5077) {
        baj += -558;m = max(m, baj)
    }
    if (sqf < 2063) {
        c -= -901;m = max(m, c)
    }
    if (jyi >= 420) {
        xml -= 572;m = max(m, xml)
    }
    if (sqf >= 2051) {
        c -= -286;m = max(m, c)
    }
    if (tme >= -1221) {
        az += 1;m = max(m, az)
    }
    if (eia > -4682) {
        c -= 551;m = max(m, c)
    }
    if (xml == 2181) {
        sz -= 261;m = max(m, sz)
    }
    if (l >= -1192) {
        b -= 354;m = max(m, b)
    }
    if (xbp != 188) {
        jyi += 34;m = max(m, jyi)
    }
    if (wfu == 935) {
        wnh += -591;m = max(m, wnh)
    }
    if (rz > 3807) {
        eia += 639;m = max(m, eia)
    }
    if (sz != 6320) {
        sqf += 211;m = max(m, sqf)
    }
    if (baj < -2241) {
        fy += 310;m = max(m, fy)
    }
    if (az < -451) {
        fy += -680;m = max(m, fy)
    }
    if (pp == -576) {
        b += -352;m = max(m, b)
    }
    if (sqf != 2280) {
        k -= -704;m = max(m, k)
    }
    if (c == 1050) {
        ugc += -664;m = max(m, ugc)
    }
    if (e < -1103) {
        eia += -501;m = max(m, eia)
    }
    if (b >= 3414) {
        u -= 943;m = max(m, u)
    }
    if (sb >= -2284) {
        baj += -20;m = max(m, baj)
    }
    if (jyi > 461) {
        eia -= 571;m = max(m, eia)
    }
    if (o > 3212) {
        u -= 642;m = max(m, u)
    }
    if (rz >= 3808) {
        b += -212;m = max(m, b)
    }
    if (wfu < 926) {
        k -= -305;m = max(m, k)
    }
    if (k >= -1784) {
        eia -= -47;m = max(m, eia)
    }
    if (sz > 6316) {
        u += -15;m = max(m, u)
    }
    if (xml <= 2184) {
        ugc += -61;m = max(m, ugc)
    }
    if (fy > 4700) {
        sz -= 663;m = max(m, sz)
    }
    if (k == -1780) {
        vke -= -340;m = max(m, vke)
    }
    if (l >= -1188) {
        eia -= 103;m = max(m, eia)
    }
    if (wfu != 928) {
        vke += -941;m = max(m, vke)
    }
    if (rz > 3813) {
        baj += 323;m = max(m, baj)
    }
    if (wnh != -1573) {
        fy -= 41;m = max(m, fy)
    }
    if (pp >= -573) {
        jyi += 20;m = max(m, jyi)
    }
    if (e != -1099) {
        o -= -656;m = max(m, o)
    }
    if (tme >= -1225) {
        sb -= 473;m = max(m, sb)
    }
    if (vke != 2797) {
        b -= 622;m = max(m, b)
    }
    if (tme < -1221) {
        c += 484;m = max(m, c)
    }
    if (y > 1780) {
        rz += 363;m = max(m, rz)
    }
    if (e != -1105) {
        b += -74;m = max(m, b)
    }
    if (k < -1784) {
        sz -= -38;m = max(m, sz)
    }
    if (xbp <= 192) {
        b -= -518;m = max(m, b)
    }
    if (sqf >= 2265) {
        xbp += -795;m = max(m, xbp)
    }
    if (baj >= -2268) {
        u += -484;m = max(m, u)
    }
    if (wfu == 935) {
        fy -= 302;m = max(m, fy)
    }
    if (wnh == -1566) {
        k -= 643;m = max(m, k)
    }
    if (xbp < -611) {
        wnh += 16;m = max(m, wnh)
    }
    if (pp >= -575) {
        eia -= 289;m = max(m, eia)
    }
    if (rz == 4175) {
        b += 673;m = max(m, b)
    }
    if (wfu <= 935) {
        wnh += 305;m = max(m, wnh)
    }
    if (wfu == 935) {
        b += 18;m = max(m, b)
    }
    if (pp == -568) {
        az += 810;m = max(m, az)
    }
    if (pp >= -559) {
        sb -= -609;m = max(m, sb)
    }
    if (ugc >= -2122) {
        sz -= 311;m = max(m, sz)
    }
    if (wnh == -1252) {
        wfu -= -836;m = max(m, wfu)
    }
    if (baj <= -2270) {
        wfu -= -78;m = max(m, wfu)
    }
    if (wfu < 1779) {
        baj -= -186;m = max(m, baj)
    }
    if (tme > -1220) {
        c -= -129;m = max(m, c)
    }
    if (tme != -1214) {
        fy -= 662;m = max(m, fy)
    }
    if (u != -2394) {
        jyi += 624;m = max(m, jyi)
    }
    if (pp != -573) {
        sb += -906;m = max(m, sb)
    }
    if (wfu >= 1780) {
        rz -= -263;m = max(m, rz)
    }
    if (xml > 2179) {
        k += -364;m = max(m, k)
    }
    if (ugc < -2117) {
        rz -= 214;m = max(m, rz)
    }
    if (xbp < -606) {
        o -= -539;m = max(m, o)
    }
    if (l <= -1188) {
        wnh += -708;m = max(m, wnh)
    }
    if (y != 1785) {
        sqf -= -901;m = max(m, sqf)
    }
    if (c != 1048) {
        ugc -= 0;m = max(m, ugc)
    }
    if (b < 4338) {
        wnh -= 712;m = max(m, wnh)
    }
    if (baj < -2086) {
        wnh -= -554;m = max(m, wnh)
    }
    if (baj == -2078) {
        sb -= 777;m = max(m, sb)
    }
    if (c > 1046) {
        l -= 713;m = max(m, l)
    }
    if (eia == -4284) {
        o += 44;m = max(m, o)
    }
    if (o != 4456) {
        rz += 77;m = max(m, rz)
    }
    if (az > 346) {
        xml += 862;m = max(m, xml)
    }
    if (k > -2151) {
        wfu -= 242;m = max(m, wfu)
    }
    if (k >= -2152) {
        b -= 998;m = max(m, b)
    }
    if (o != 4460) {
        sz -= 202;m = max(m, sz)
    }
    if (wfu < 1537) {
        tme += 941;m = max(m, tme)
    }
    if (c == 1050) {
        pp += -536;m = max(m, pp)
    }
    if (y != 1781) {
        wfu += 557;m = max(m, wfu)
    }
    if (y != 1783) {
        e += -982;m = max(m, e)
    }
    if (y != 1790) {
        u -= -49;m = max(m, u)
    }
    if (jyi <= 1102) {
        az += 602;m = max(m, az)
    }
    if (c != 1040) {
        e += -457;m = max(m, e)
    }
    if (tme < -279) {
        wnh += 914;m = max(m, wnh)
    }
    if (c == 1050) {
        tme += -262;m = max(m, tme)
    }
    if (c == 1050) {
        c += -611;m = max(m, c)
    }
    if (az >= 953) {
        xml += 921;m = max(m, xml)
    }
    if (k > -2148) {
        tme += -910;m = max(m, tme)
    }
    if (o >= 4452) {
        xml += 994;m = max(m, xml)
    }
    if (xbp == -612) {
        c += 433;m = max(m, c)
    }
    if (jyi == 1101) {
        wfu -= 591;m = max(m, wfu)
    }
    if (wnh < -1045) {
        jyi -= -916;m = max(m, jyi)
    }
    if (jyi == 2007) {
        eia -= -514;m = max(m, eia)
    }
    if (o <= 4463) {
        tme -= -305;m = max(m, tme)
    }
    if (baj <= -2076) {
        c -= -404;m = max(m, c)
    }
    if (wfu > 1501) {
        rz -= -942;m = max(m, rz)
    }
    if (k == -2144) {
        xbp += 286;m = max(m, xbp)
    }
    if (fy >= 3743) {
        xbp += -119;m = max(m, xbp)
    }
    if (baj != -2074) {
        ugc += 171;m = max(m, ugc)
    }
    if (xbp < -443) {
        l -= -784;m = max(m, l)
    }
    if (sqf < 3173) {
        ugc -= 146;m = max(m, ugc)
    }
    if (c > 1272) {
        c -= 397;m = max(m, c)
    }
    if (fy != 3743) {
        k -= 468;m = max(m, k)
    }
    if (sz == 5145) {
        wnh += -174;m = max(m, wnh)
    }
    if (eia == -4284) {
        u -= -225;m = max(m, u)
    }
    if (fy == 3743) {
        sqf += 687;m = max(m, sqf)
    }
    if (jyi == 2008) {
        e += 705;m = max(m, e)
    }
    if (c <= 887) {
        wfu += -904;m = max(m, wfu)
    }
    if (wfu <= 597) {
        jyi -= -593;m = max(m, jyi)
    }
    if (e < -1554) {
        tme += 807;m = max(m, tme)
    }
    if (az > 946) {
        fy += -497;m = max(m, fy)
    }
    if (sz == 5146) {
        rz += -921;m = max(m, rz)
    }
    if (wfu >= 591) {
        az -= -401;m = max(m, az)
    }
    if (k > -2149) {
        sz -= 728;m = max(m, sz)
    }
    if (vke <= 2787) {
        c -= 884;m = max(m, c)
    }
    if (vke <= 2804) {
        l -= 764;m = max(m, l)
    }
    if (b < 3346) {
        xml -= 226;m = max(m, xml)
    }
    if (c < 879) {
        sqf -= -84;m = max(m, sqf)
    }
    if (e <= -1556) {
        eia += 634;m = max(m, eia)
    }
    if (az >= 1357) {
        o += -473;m = max(m, o)
    }
    if (o == 3988) {
        sb += 917;m = max(m, sb)
    }
    if (tme >= -334) {
        ugc += 411;m = max(m, ugc)
    }
    if (az != 1353) {
        jyi += 214;m = max(m, jyi)
    }
    if (c <= 879) {
        xbp += -296;m = max(m, xbp)
    }
    if (sb < -3525) {
        k += -416;m = max(m, k)
    }
    if (tme != -350) {
        eia += -847;m = max(m, eia)
    }
    if (k <= -2139) {
        sb -= 525;m = max(m, sb)
    }
    if (sz > 4415) {
        sqf += -749;m = max(m, sqf)
    }
    if (xbp != -749) {
        c += -382;m = max(m, c)
    }
    if (wfu < 594) {
        l -= -705;m = max(m, l)
    }
    if (xbp >= -739) {
        sz -= 38;m = max(m, sz)
    }
    if (xbp != -733) {
        sqf -= 431;m = max(m, sqf)
    }
    if (xbp >= -748) {
        b += 766;m = max(m, b)
    }
    if (tme != -341) {
        wfu += -263;m = max(m, wfu)
    }
    if (c != 494) {
        wnh += 435;m = max(m, wnh)
    }
    if (vke != 2807) {
        sqf -= -207;m = max(m, sqf)
    }
    if (sz < 4419) {
        sb += -192;m = max(m, sb)
    }
    if (wfu > 319) {
        sb += 985;m = max(m, sb)
    }
    if (ugc != -2097) {
        wfu += 478;m = max(m, wfu)
    }
    if (rz < 4042) {
        eia += -44;m = max(m, eia)
    }
    if (jyi >= 2818) {
        u += 457;m = max(m, u)
    }
    if (sb == -3249) {
        sb += 929;m = max(m, sb)
    }
    if (vke >= 2793) {
        vke -= -199;m = max(m, vke)
    }
    if (u < -1656) {
        sqf -= -144;m = max(m, sqf)
    }
    if (ugc == -2097) {
        sqf -= -917;m = max(m, sqf)
    }
    if (b == 4109) {
        rz -= 958;m = max(m, rz)
    }
    if (vke <= 2996) {
        k += -532;m = max(m, k)
    }
    if (sqf <= 3804) {
        y -= -150;m = max(m, y)
    }
    if (u == -1651) {
        jyi -= -149;m = max(m, jyi)
    }
    if (wfu < 337) {
        jyi -= 939;m = max(m, jyi)
    }
    if (wnh <= -782) {
        xml += 266;m = max(m, xml)
    }
    if (tme == -338) {
        pp += 130;m = max(m, pp)
    }
    if (xml > 4994) {
        u += 665;m = max(m, u)
    }
    if (tme >= -341) {
        o += -510;m = max(m, o)
    }
    if (sz != 4421) {
        k -= -273;m = max(m, k)
    }
    if (wnh < -778) {
        vke += 392;m = max(m, vke)
    }
    if (wfu <= 334) {
        pp -= -464;m = max(m, pp)
    }
    if (xbp != -741) {
        xml -= 746;m = max(m, xml)
    }
    if (eia != -4541) {
        xml -= 215;m = max(m, xml)
    }
    if (sz > 4420) {
        b += 26;m = max(m, b)
    }
    if (l != -1183) {
        wnh += -601;m = max(m, wnh)
    }
    if (xbp < -744) {
        u -= -660;m = max(m, u)
    }
    if (xml < 5004) {
        o -= -362;m = max(m, o)
    }
    if (y > 1924) {
        k -= 689;m = max(m, k)
    }
    if (sb <= -2315) {
        ugc -= -245;m = max(m, ugc)
    }
    if (o >= 3835) {
        rz += 746;m = max(m, rz)
    }
    if (baj == -2078) {
        sqf += -65;m = max(m, sqf)
    }
    if (az > 1365) {
        xml -= -571;m = max(m, xml)
    }
    if (wnh < -1379) {
        xml += -309;m = max(m, xml)
    }
    if (y == 1933) {
        wnh -= -554;m = max(m, wnh)
    }
    if (xbp == -741) {
        vke += 542;m = max(m, vke)
    }
    if (o == 3840) {
        e += -386;m = max(m, e)
    }
    if (c > 495) {
        b += -605;m = max(m, b)
    }
    if (sz <= 4426) {
        az += -784;m = max(m, az)
    }
    if (sz < 4418) {
        pp += 308;m = max(m, pp)
    }
    if (eia < -4549) {
        c -= -79;m = max(m, c)
    }
    if (b >= 3497) {
        sb += 224;m = max(m, sb)
    }
    if (az <= 578) {
        rz -= 681;m = max(m, rz)
    }
    if (c <= 500) {
        ugc += 210;m = max(m, ugc)
    }
    if (o < 3834) {
        baj += -271;m = max(m, baj)
    }
    if (sz < 4422) {
        l += 476;m = max(m, l)
    }
    if (tme >= -345) {
        o -= -432;m = max(m, o)
    }
    if (u < -982) {
        baj += -713;m = max(m, baj)
    }
    if (baj == -2791) {
        u += -192;m = max(m, u)
    }
    if (wnh <= -833) {
        baj -= 411;m = max(m, baj)
    }
    if (y == 1933) {
        pp -= 728;m = max(m, pp)
    }
    if (sb != -2096) {
        ugc += -200;m = max(m, ugc)
    }
    if (fy == 3252) {
        l -= 787;m = max(m, l)
    }
    if (rz >= 3136) {
        pp -= -924;m = max(m, pp)
    }
    if (o <= 4280) {
        o += 792;m = max(m, o)
    }
    if (u < -1171) {
        b -= -300;m = max(m, b)
    }
    if (az >= 580) {
        sqf -= -328;m = max(m, sqf)
    }
    if (baj >= -2799) {
        vke -= 887;m = max(m, vke)
    }
    if (vke == 3050) {
        u += -252;m = max(m, u)
    }
    if (eia <= -4541) {
        o -= 640;m = max(m, o)
    }
    if (wnh >= -836) {
        sb += -919;m = max(m, sb)
    }
    if (wnh <= -841) {
        l -= 463;m = max(m, l)
    }
    if (b <= 3797) {
        rz -= -942;m = max(m, rz)
    }
    if (fy <= 3253) {
        az -= -170;m = max(m, az)
    }
    if (eia > -4545) {
        e += 718;m = max(m, e)
    }
    if (ugc <= -1650) {
        az -= 103;m = max(m, az)
    }
    if (jyi > 1879) {
        sqf += 709;m = max(m, sqf)
    }
    if (c == 502) {
        pp += 566;m = max(m, pp)
    }
    if (sz <= 4417) {
        e += -611;m = max(m, e)
    }
    if (wfu != 328) {
        baj += -998;m = max(m, baj)
    }
    if (sqf < 4450) {
        o -= 891;m = max(m, o)
    }
    if (pp <= -143) {
        sz -= -86;m = max(m, sz)
    }
    if (sz > 4411) {
        xbp -= 199;m = max(m, xbp)
    }
    if (c != 487) {
        rz += -158;m = max(m, rz)
    }
    if (wfu > 324) {
        rz += -710;m = max(m, rz)
    }
    if (fy >= 3241) {
        ugc -= -635;m = max(m, ugc)
    }
    if (az <= 738) {
        k += -221;m = max(m, k)
    }
    if (e > -1839) {
        sqf += -74;m = max(m, sqf)
    }
    if (az >= 743) {
        az -= -317;m = max(m, az)
    }
    if (tme <= -339) {
        eia -= 596;m = max(m, eia)
    }
    if (wnh == -832) {
        vke -= 708;m = max(m, vke)
    }
    if (sz == 4417) {
        u -= 414;m = max(m, u)
    }
    if (e < -1841) {
        y += 388;m = max(m, y)
    }
    if (y == 1941) {
        xml += -783;m = max(m, xml)
    }
    if (c < 498) {
        o -= 695;m = max(m, o)
    }
}
fun five() {
    if (baj != -2787) {
        ugc -= -944;m = max(m, ugc)
    }
    if (baj <= -2787) {
        b += -964;m = max(m, b)
    }
    if (sqf == 4373) {
        vke -= -208;m = max(m, vke)
    }
    if (rz >= 2274) {
        vke += -206;m = max(m, vke)
    }
    if (tme < -333) {
        ugc += 726;m = max(m, ugc)
    }
    if (ugc < 667) {
        eia += 986;m = max(m, eia)
    }
    if (vke < 2343) {
        baj -= -139;m = max(m, baj)
    }
    if (eia != -4156) {
        o -= -19;m = max(m, o)
    }
    if (sz > 4419) {
        wnh += -396;m = max(m, wnh)
    }
    if (rz >= 2277) {
        ugc += -74;m = max(m, ugc)
    }
    if (wnh < -828) {
        pp += 239;m = max(m, pp)
    }
    if (u == -1594) {
        sz += -741;m = max(m, sz)
    }
    if (k < -3089) {
        c += 340;m = max(m, c)
    }
    if (sqf >= 4371) {
        xml -= 156;m = max(m, xml)
    }
    if (b != 2841) {
        sqf -= 433;m = max(m, sqf)
    }
    if (tme < -334) {
        vke -= 231;m = max(m, vke)
    }
    if (eia > -4150) {
        u += 393;m = max(m, u)
    }
    if (u >= -1599) {
        xbp -= -482;m = max(m, xbp)
    }
    if (u > -1601) {
        fy += 40;m = max(m, fy)
    }
    if (sz <= 3684) {
        baj -= 29;m = max(m, baj)
    }
    if (xml != 4531) {
        sz -= 874;m = max(m, sz)
    }
    if (pp <= 103) {
        ugc += -50;m = max(m, ugc)
    }
    if (xbp == -458) {
        sb -= 96;m = max(m, sb)
    }
    if (jyi != 1894) {
        e += 266;m = max(m, e)
    }
    if (y == 1933) {
        sz += 336;m = max(m, sz)
    }
    if (wfu > 321) {
        eia -= -592;m = max(m, eia)
    }
    if (o > 2863) {
        rz -= -580;m = max(m, rz)
    }
    if (l != -701) {
        xml -= -468;m = max(m, xml)
    }
    if (sqf == 3940) {
        sqf += -672;m = max(m, sqf)
    }
    if (jyi < 1891) {
        vke -= -600;m = max(m, vke)
    }
    if (baj < -2677) {
        e -= -31;m = max(m, e)
    }
    if (rz != 2275) {
        rz -= -409;m = max(m, rz)
    }
    if (ugc > 536) {
        jyi += 839;m = max(m, jyi)
    }
    if (c > 830) {
        wfu += -886;m = max(m, wfu)
    }
    if (l == -708) {
        vke += 500;m = max(m, vke)
    }
    if (tme >= -344) {
        sz += 490;m = max(m, sz)
    }
    if (rz != 2684) {
        rz += -478;m = max(m, rz)
    }
    if (o != 2853) {
        sqf += -367;m = max(m, sqf)
    }
    if (u <= -1585) {
        baj += -461;m = max(m, baj)
    }
    if (c <= 827) {
        fy -= -642;m = max(m, fy)
    }
    if (b > 2836) {
        ugc += -371;m = max(m, ugc)
    }
    if (jyi <= 2727) {
        l += -983;m = max(m, l)
    }
    if (wfu <= -565) {
        b -= 599;m = max(m, b)
    }
    if (baj <= -3149) {
        rz += -852;m = max(m, rz)
    }
    if (c != 827) {
        sz -= -453;m = max(m, sz)
    }
    if (jyi < 2725) {
        vke += 631;m = max(m, vke)
    }
    if (xml >= 4996) {
        b += 145;m = max(m, b)
    }
    if (ugc == 168) {
        sqf += -921;m = max(m, sqf)
    }
    if (xml < 5002) {
        c += 933;m = max(m, c)
    }
    if (y <= 1934) {
        l -= 994;m = max(m, l)
    }
    if (wnh <= -828) {
        eia -= 59;m = max(m, eia)
    }
    if (ugc >= 166) {
        wfu -= 600;m = max(m, wfu)
    }
    if (az > 1050) {
        b -= -316;m = max(m, b)
    }
    if (y < 1940) {
        sz -= 832;m = max(m, sz)
    }
    if (fy >= 3280) {
        fy -= -517;m = max(m, fy)
    }
    if (jyi <= 2727) {
        c += -801;m = max(m, c)
    }
    if (xml < 5007) {
        xbp -= 381;m = max(m, xbp)
    }
    if (baj >= -3142) {
        rz -= 115;m = max(m, rz)
    }
    if (tme > -332) {
        az += -553;m = max(m, az)
    }
    if (rz <= 2093) {
        wfu += 837;m = max(m, wfu)
    }
    if (tme <= -334) {
        ugc += -490;m = max(m, ugc)
    }
    if (xbp >= -843) {
        fy += 645;m = max(m, fy)
    }
    if (tme >= -341) {
        tme += -756;m = max(m, tme)
    }
    if (e != -1543) {
        rz += -987;m = max(m, rz)
    }
    if (jyi > 2715) {
        xml += -423;m = max(m, xml)
    }
    if (c <= 975) {
        rz += 985;m = max(m, rz)
    }
    if (o > 2847) {
        ugc += -698;m = max(m, ugc)
    }
    if (l <= -2684) {
        rz -= 498;m = max(m, rz)
    }
    if (xbp != -845) {
        baj -= -826;m = max(m, baj)
    }
    if (o >= 2849) {
        k += 829;m = max(m, k)
    }
    if (az == 1060) {
        xbp += 632;m = max(m, xbp)
    }
    if (baj >= -2321) {
        ugc += 778;m = max(m, ugc)
    }
    if (sz == 3249) {
        y += -806;m = max(m, y)
    }
    if (y != 1120) {
        baj -= -91;m = max(m, baj)
    }
    if (fy == 4448) {
        xml -= 932;m = max(m, xml)
    }
    if (xbp != -207) {
        fy += -682;m = max(m, fy)
    }
    if (u > -1602) {
        eia += -640;m = max(m, eia)
    }
    if (baj == -2225) {
        l += -736;m = max(m, l)
    }
    if (l >= -3429) {
        xbp += 244;m = max(m, xbp)
    }
    if (sqf < 1974) {
        pp += 26;m = max(m, pp)
    }
    if (e != -1541) {
        wfu += -465;m = max(m, wfu)
    }
    if (l != -3426) {
        e -= -224;m = max(m, e)
    }
    if (sb > -3117) {
        wfu -= 424;m = max(m, wfu)
    }
    if (u > -1597) {
        wfu += 699;m = max(m, wfu)
    }
    if (b > 3298) {
        ugc -= -222;m = max(m, ugc)
    }
    if (c > 967) {
        c -= 57;m = max(m, c)
    }
    if (fy > 4440) {
        e += 318;m = max(m, e)
    }
    if (pp != 102) {
        jyi -= -93;m = max(m, jyi)
    }
    if (xbp < 34) {
        jyi += 71;m = max(m, jyi)
    }
    if (xbp <= 42) {
        pp += 598;m = max(m, pp)
    }
    if (wnh == -832) {
        az += 143;m = max(m, az)
    }
    if (baj > -2227) {
        xml -= 13;m = max(m, xml)
    }
    if (az >= 1200) {
        ugc += -643;m = max(m, ugc)
    }
}
one()
two()
three()
four()
five()
println(m)

